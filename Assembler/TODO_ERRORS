PAS DE .COR

pas de name / pas de comment (il peut y avoir une ligne vide avant ou comment mais rien dautre);
pas de commande;
commande avec un argument incompatible || avec trop d'argument;
pas de virgule apres qu'on ai tous les args;
ligne de merde avec des chars =>erreur;



ON CREER LE .COR

gestion espaces / tabs entre deux params;
ligne de merde avec que des ints et "';', ':', '"', '#', ',', " => on stop la lecture mais pas d'erreur;
il peut y avoir un commentaire apres une ligne opcode;
